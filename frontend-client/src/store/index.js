// Puedes configurar aquí Vuex o Pinia según prefieras.
// Este es un ejemplo base con Vuex.
import { createStore } from 'vuex';

const store = createStore({
  modules: {
    // auth, user, raffle, etc.
  },
});

export default store;
